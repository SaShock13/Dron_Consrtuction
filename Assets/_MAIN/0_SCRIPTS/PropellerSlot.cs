using UnityEngine;

public class PropellerSlot 
{
    //private DronePart[] attachedParts;
    //private Transform[] attachTransforms;
    //public override void Attach(DronePart part)
    //{
    //    if (IsSlotEmpty && part.Type == compatiblePart)
    //    {
    //        for (int i = 0; i < attachTransforms.Length; i++)
    //        {
    //            attachedParts[i].transform.parent = attachTransforms[i];
    //            attachedParts[i].transform.localPosition = part.data.localPositionOffset;
    //            attachedParts[i].transform.localEulerAngles = part.data.localRotationOffset;

    //        }
    //        attachedPart = part;

    //    }
        
    //}
}
